export interface UserInfoProps {
  id: string;
  firstName: string;
  lastName: string;
  accessToken: string;
  refreshToken: string;
  gender: string;
  image: string;
  email: string;
  username: string;
}
